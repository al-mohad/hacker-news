enum ViewState { idle, busy }

enum AuthState {
  emailCheck,
  emailVerification,
  signIn,
  signUp,
  forgotPassword,
  resetPassword,
}

enum OnBoardingState { selectTheme, selectLanguage, onBoarding }
